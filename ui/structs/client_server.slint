// no connection with other ClientServer because there cannot be a direct connection
export struct ClientServerStruct {
    id : int,
    drones_adjacent: [int],
    drones_not_adjacent: [int],
    position_in_vector: int,
}